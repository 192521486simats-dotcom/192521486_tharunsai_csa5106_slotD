#include <stdio.h>
#include <math.h>

int main() {
    int i;
    double log2_fact = 0;

    // Calculate log2(25!)
    for(i = 1; i <= 25; i++) {
        log2_fact += log2(i);
    }

    printf("Approximate number of Playfair keys:\n");
    printf("25! ≈ 2^%.0f\n", log2_fact);

    return 0;
}
